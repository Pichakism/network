# Exercise 02 - Static IP Configuration with Netplan

This exercise demonstrates how to configure a static IP address using Netplan. The configuration defines a fixed IP address, subnet mask, default gateway, and DNS servers. Static addressing is commonly used for servers and network infrastructure devices.